namespace CleanSlice.Domain.Common.Enums;

public enum LoginProvider
{
    Local = 1,           // Local authentication (username/password)
    Google = 2,          // Google OAuth
    Microsoft = 3,       // Microsoft OAuth
}
